je suis edit user
<form action="/user-profile/edit/{{user._id}}" method="post">
    <label for="name">edit name</label>
    <input id="name" type="text" name="name" value="{{user.name}}">
    <label for="email">edit email</label>
    <input id="email" type="text" name="email" value="{{user.email}}">
    <label for="age">edit age</label>
    <input id="age" type="number" name="age" value="{{user.age}}">
    <label for="field">edit field</label>
    <input id="field" type="text" name="field" value="{{user.field}}">
    <label for="skills">edit skills</label>
    <input id="skills" type="text" name="skills" value="{{user.skills}}">
    <label for="website">edit website</label>
    <input id="website" type="text" name="website" value="{{user.website}}">
    <label for="birthday">edit birthday</label>
    <input id="birthday" type="text" name="birthday" value="{{user.birthday}}">
    <button>edit</button>
</form>